import { describe, expect, it } from 'vitest'

describe('Solitaire Infrastructure', () => {
  it('should have a working test setup', () => {
    expect(true).toBe(true)
  })

  it('should support basic arithmetic (sanity check)', () => {
    expect(1 + 1).toBe(2)
  })
})
